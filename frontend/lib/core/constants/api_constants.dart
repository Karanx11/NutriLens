import 'package:flutter/material.dart';

class ApiConstants extends StatelessWidget {
  const ApiConstants({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
